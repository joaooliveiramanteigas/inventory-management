export type CustomChartData = {
  labels: string[];
  datasets: {
    label: string;
    data: number[];
    backgroundColor: string[];
  }[];
};
export type ProductData = {
  label: string;
  value: number;
  backgroundColor: string;
};
